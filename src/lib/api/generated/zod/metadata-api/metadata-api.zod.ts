/**
 * Generated by orval v8.9.1 🍺
 * Do not edit manually.
 * Harune API
 * API documentation for Harune
 * OpenAPI spec version: 1.0.0
 */
import * as zod from "zod";

/**
 * Returns metadata extracted from the target URL.

Rules:
- `domain` is the normalized hostname from the resolved URL with common prefixes like `www.` removed
- Supported provider URLs return the canonical R2 favicon URL instead of a fetched page favicon
- GitHub profile URLs also include a provider metadata payload with the recent 60-day contribution calendar
- GitHub enrichment requires `GITHUB_TOKEN`
- Twitch channel URLs also include a provider metadata payload from Helix `users` plus `channels/followers` and return the channel's follower count when `TWITCH_CLIENT_ID` and `TWITCH_USER_ACCESS_TOKEN` are configured
- Twitch responses narrow `providerMetadata` to `provider: "twitch"`, `viewType: "twitch_channel"`, and a payload containing broadcaster info plus `followerCount`
- Discord invite URLs also include a provider metadata payload from `invites/{code}?with_counts=true` and return the guild member count
- Discord responses narrow `providerMetadata` to `provider: "discord"`, `viewType: "discord_invite"`, and a payload containing invite, guild, and count fields
- YouTube channel URLs also include a provider metadata payload from `channels.list` and the server tries `id`, `forHandle`, and `forUsername` until one returns a channel
- YouTube responses narrow `providerMetadata` to `provider: "youtube"`, `viewType: "youtube_channel"`, and a payload containing `snippet` and `statistics`
- YouTube video URLs also include a provider metadata payload from `videos.list` and the server uses `player.embedHtml` with the fetched video snippet and statistics
- YouTube video responses narrow `providerMetadata` to `provider: "youtube"`, `viewType: "youtube_video"`, and a payload containing `snippet`, `player`, and `statistics`
- YouTube enrichment requires `YOUTUBE_API_KEY`
- Spotify URLs on `open.spotify.com` and `spotify.link` are treated as oEmbed candidates; the server only uses Spotify oEmbed when the page exposes an `application/json+oembed` link, and otherwise falls back to generic HTML metadata extraction
- Spotify oEmbed responses narrow `providerMetadata` to `provider: "spotify"`, `viewType: "spotify_oembed"`, and a payload containing the oEmbed title, embed HTML, size, provider, and thumbnail fields
- Non-provider URLs keep the generic metadata response and leave provider fields null
 * @summary Fetch metadata for a URL
 */
export const GetMetadataQueryParams = zod.object({
  url: zod.string().describe("URL to fetch metadata from."),
});

export const GetMetadataResponse = zod.object({
  url: zod.string(),
  domain: zod.string(),
  title: zod.string().nullable(),
  description: zod.string().nullable(),
  image: zod.string().nullable(),
  siteName: zod.string().nullable(),
  favicon: zod.string().nullable(),
  provider: zod.string().nullable(),
  providerMetadata: zod
    .object({
      provider: zod.string(),
      viewType: zod.string(),
      fetchedAt: zod.string(),
      payload: zod.record(zod.string(), zod.unknown()),
    })
    .nullable(),
});
